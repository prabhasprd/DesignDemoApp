export interface AppconfigLanguageType {
    type: string;
    payload: any;
}
export interface UserData {
    name: string;
    phone: string;
    email: string;
    password: string;
    country: string;
}
